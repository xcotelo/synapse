logging:
  level:
    org:
      hibernate:  
        SQL: debug
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/synapse
    driver-class-name: org.postgresql.Driver
    username: synapse
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update   # o create-drop si quieres recrear cada vez
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 50MB     # Tamaño máximo de cada archivo
      max-request-size: 50MB   # Tamaño máximo de la solicitud
server:
  servlet:
    context-path: @project.contextPath@
project:
  jwt:
    signKey: WzROun12eUiDvko3UrJ74gNeJz2TWEWPMTh6DxyE8HWXIf4hETUrkM4IUGqqHq66PuVS
    expirationMinutes: 1440
  llama:
    apiKey: ${API_KEY_LLAMA}
    apiUrl: https://api.groq.com/openai/v1/chat/completions
    model: llama-3.3-70b-versatile
  # Carpeta local donde se guardan las notas procesadas en formato Markdown.
  # Es intencionalmente "Git-friendly" (almacenamiento abierto y versionable).
  notes:
    dir: digital-brain-notes

